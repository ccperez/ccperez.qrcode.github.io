const wrapper=document.querySelector(".wrapper"),qrInput=wrapper.querySelector(".form input"),generateBtn=wrapper.querySelector(".form button");let preValue,qrcode=new QRCode(document.querySelector("#qrcode"),{width:200,height:200,colorDark:"#000000",colorLight:"#ffffff",correctLevel:QRCode.CorrectLevel.H});generateBtn.addEventListener("click",()=>{let a=qrInput.value.trim();function b(){wrapper.classList.add("active"),qrcode.makeCode(a),generateBtn.innerText="Generate QR Code"}a&&preValue!==a&&(preValue=a,getWithExpiry("kyXprd")?generateBtn.innerText="Expired to Generate QR Code ":(generateBtn.innerText="Generating QR Code...",setTimeout(b,500)))}),qrInput.addEventListener("keyup",()=>{qrInput.value.trim()||(wrapper.classList.remove("active"),preValue="")})